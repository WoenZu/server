'use strict';





function MessageConstructor() {
  this.key = 'key';
}

MessageConstructor.prototype.createMessage = function( command, parameters ){
  var message = fabric.createMessage( command, parameters );
  //return = encoder.encode( message );
  return message;
};

exports.MessageConstructor = MessageConstructor;